[build]
  command = "echo 'Skip build'"
  publish = "dist/weather-app"

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Note: Scheduled functions configured via Netlify UI
# Go to: Site → Functions → daily-weather-email → Schedule
# Set cron: 30 0 * * * (6:00 AM IST)
